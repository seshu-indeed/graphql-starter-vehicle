type Expense {
    amount: Int,
    categoryCode: String,
}

type Category {
    code: String,
    description: String,
}

type Query {
    expenses:[Expense]
    categories: [Category]
}

type Mutation {
    createExpense(amount: Int, categoryCode: String!):Expense
    createCategory(code: String!, description: String!):Category
}